# final-project-dsc148

Data obtained from this link: https://www.kaggle.com/datasets/ealtman2019/credit-card-transactions?utm_source=chatgpt.com&select=User0_credit_card_transactions.csv.

The zip file was too large to push to github, but you can access it here. The csv file I used is "credit_card_transactions-ibm_v2.csv". The file that I did upload to the git repository was a smaller csv file from the same kaggle link. This is not the csv I used in my project, but the inital columns and data are similar, so it can be used as a reference.

I also wanted to note that my all the code in my jupyter notebook works, but I was in the process of running certain parts to fill out my report, and I hadn't fully re-run all of the code when I uploaded the ipynb file to the repository, so some of the outputs look incomplete, however if the code is restarted and run in order it all runs nicely. I figured that only the code will be looked at, but I thought that this was worth pointing out just in case.
